---
title: What is the process for changing values within the postgresql JSON data type?
authors:
- cool_wizard
tags:
- json
- knowledge
thumbnail: images/json.png
created_at: 2023-02-08 00:00:00
updated_at: 2023-02-08 00:00:00
tldr: Use the JSONB operators/functions to modify fields inside the new PostgreSQL JSON datatype.
---

**Contents**

[TOC]

**Section 1 - Accessing Data**

The easiest way to access data inside a PostgreSQL JSON datatype is to use the `->` operator. This operator can be used to access a single element inside a JSON object or array, or to access a single element inside a JSON object or array.

**Section 2 - Modifying Data**

Once you have accessed the data, you can modify it by using the `->>` operator. This operator can be used to update a single element inside a JSON object or array.

**Section 3 - Adding Data**

If you want to add new data to a PostgreSQL JSON datatype, you can use the `->>` operator. This operator can be used to add a new key-value pair to a JSON object or array.

**Section 4 - Removing Data**

Finally, you can remove data from a PostgreSQL JSON datatype by using the `-#` operator. This operator can be used to remove a single element from a JSON object or array.
